<!DOCTYPE html>
<html lang="en">

<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Event Page</title>

<head>
    <%- include('./partials/head') %>
</head>

<body>
    <%- include('./partials/nav') %>
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="mt-3">
                    <%= eventName %>
                </h1>
                <!-- <img src="<%= blog.coverImageURL %>" class="img-fluid" alt="Blog cover image"> -->
                <pre><%= event.eventDescription %></pre>
            </div>
        </div>
        <div class="row mt-4">
            <div class="col-1">
                <!-- <img src=" class="img-fluid rounded-circle" alt="Profile picture"> -->
            </div>
            <!-- <div class="col">
                
            </div> -->
        </div>

        <div class="row mt-3">
            <div class="col-12">
                <!-- <div class="form-text"> Comments :  <%= comments.length %> </div> -->

                <!-- <% if (locals.user) { %>
                    <form action="/blog/comment/<%= blog._id %>" method="post">
                        <div class="m-3">
                            <input type="text" name="content" class="form-control" id="exampleInputEmail1"
                                aria-describedby="emailHelp" placeholder="Enter Your Comment">
                            <div id="emailHelp" class="form-text">Comment Here.</div>
                        </div>
                        <button type="submit" class="btn btn-primary">Add</button>
                    </form>
                <% } %> -->
            </div>
        </div>

        <!-- <div class="mt-4">
            <% comments.forEach(comment=>{ %>
                <div class="row">
                    <div class="col-1">
                        <img src="<%= comment.createdBy.profilePicture %>" class="img-fluid rounded-circle" alt="Profile picture">
                    </div>
                    <div class="col">
                        <h5><pre><%= comment.createdBy.firstName %></pre></h5>
                        <p><%= comment.content %></p>
                    </div>
                </div>
            <% }) %>
        </div> -->
    </div>
    <%- include('./partials/scripts') %>
</body>

</html>